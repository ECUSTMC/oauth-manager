{% extends 'user.base' %}

{% block title %}{{ trans('OAuthRecord::oauth-record.hall-title') }}{% endblock %}

{% block content %}
<div id="oauth-hall">
  {% if apps is empty %}
    <div class="card">
      <div class="card-body text-center py-5">
        <i class="fas fa-store fa-3x text-muted mb-3"></i>
        <h5>{{ trans('OAuthRecord::oauth-record.hall-empty-title') }}</h5>
        <p class="text-muted">{{ trans('OAuthRecord::oauth-record.hall-empty-desc') }}</p>
      </div>
    </div>
  {% else %}
    <div class="row">
      {% for app in apps %}
      <div class="col-md-4 mb-3">
        <div class="card h-100">
          <div class="card-body">
            <div class="d-flex align-items-center mb-2">
              <div class="mr-3" style="width: 40px; height: 40px; flex-shrink: 0; position: relative;">
                <div class="bg-light rounded-circle d-flex align-items-center justify-content-center fallback-icon"
                     style="width: 40px; height: 40px; position: absolute; top: 0; left: 0;">
                  <i class="fas fa-plug text-secondary"></i>
                </div>
                {% if app.url %}
                <img class="app-favicon rounded" alt=""
                     data-url="{{ app.url }}"
                     style="width: 40px; height: 40px; display: none; object-fit: contain; position: absolute; top: 0; left: 0; z-index: 1; background: #fff; border-radius: 8px;">
                {% endif %}
              </div>
              <div style="min-width: 0; flex: 1;">
                <h6 class="mb-0 text-truncate">{{ app.name }}</h6>
                <div class="text-truncate" style="font-size: 0.8rem;">
                  {% if app.url %}
                  <a href="{{ app.url }}" target="_blank" rel="noopener noreferrer" class="text-muted">
                    {{ app.domain }}
                  </a>
                  {% elseif app.domain %}
                  <span class="text-muted">{{ app.domain }}</span>
                  {% endif %}
                </div>
              </div>
            </div>
            <div class="d-flex justify-content-between align-items-center mt-3">
              <div>
                <small class="text-muted mr-2">
                  <i class="fas fa-users"></i>
                  {{ trans('OAuthRecord::oauth-record.hall-user-count', {count: app.user_count}) }}
                </small>
                {% if app.creator %}
                <small class="text-muted">
                  <i class="fas fa-user"></i>
                  {{ app.creator }}
                </small>
                {% endif %}
              </div>
              {% if app.authorized %}
              <span class="badge badge-success">
                <i class="fas fa-check"></i>
                {{ trans('OAuthRecord::oauth-record.hall-authorized') }}
              </span>
              {% else %}
              <span class="badge badge-secondary">
                {{ trans('OAuthRecord::oauth-record.hall-not-authorized') }}
              </span>
              {% endif %}
            </div>
          </div>
        </div>
      </div>
      {% endfor %}
    </div>
  {% endif %}
</div>

<style>
#oauth-hall .card {
  transition: box-shadow 0.2s ease;
}
#oauth-hall .card:hover {
  box-shadow: 0 2px 8px rgba(0,0,0,.12);
}
#oauth-hall .badge {
  font-size: 0.75rem;
}
#oauth-hall a.text-muted:hover {
  text-decoration: underline;
}
</style>
{% endblock %}

{% block before_foot %}
<script>
(function() {
  var favicons = document.querySelectorAll('.app-favicon');

  favicons.forEach(function(img) {
    var url = img.dataset.url;
    if (!url) return;

    function showFavicon(src) {
      img.onload = function() {
        img.style.display = 'block';
      };
      img.onerror = function() {
        img.style.display = 'none';
      };
      img.src = src;
      if (img.complete) {
        img.style.display = img.naturalWidth > 0 ? 'block' : 'none';
      }
    }

    // Step 1: Browser tries /favicon.ico directly
    showFavicon(url + '/favicon.ico');

    // Step 2: If /favicon.ico fails, ask server to parse <link rel="icon">
    img.onerror = function() {
      fetch('/oauth-apps/favicon?url=' + encodeURIComponent(url))
        .then(function(resp) { return resp.json(); })
        .then(function(data) {
          if (data.favicon && data.favicon !== (url + '/favicon.ico')) {
            showFavicon(data.favicon);
          } else {
            img.style.display = 'none';
          }
        })
        .catch(function() {
          img.style.display = 'none';
        });
    };
  });
})();
</script>
{% endblock %}
